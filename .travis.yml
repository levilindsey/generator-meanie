language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-meanie' ]; then cd .. && eval "mv $currentfolder generator-meanie" && cd generator-meanie; fi

